import { Image as ImageComponent } from "react-bootstrap"

import arrowIcon from "public/Home/arrow-icon.svg"

const ArrowIcon = () => (
  <ImageComponent alt="Flecha" height={24} src={arrowIcon} width={24} />
)

export default ArrowIcon
